# Python
__pycache__/
*.pyc
.venv/
venv/
*.db
# Node / Vite
node_modules/
dist/
.vite/
# Editors / OS
.DS_Store
.idea/
.vscode/
# Crash dumps
core.[0-9]*
# Local-only infra/debugging notes (see TUTOR_PROMPT.md Session protocol) — never pushed
ENVIRONMENT_LOG.md
.env
# Lambda deploy build artifacts (regenerate via pip install -t + zip, not committed)
backend/package/
backend/function.zip
backend/response.json
